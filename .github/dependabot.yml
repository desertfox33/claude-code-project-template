version: 2
updates:
  # Enable package ecosystem entries only when the repository adds real dependency files.
  # Examples:
  #
  # - package-ecosystem: "github-actions"
  #   directory: "/"
  #   schedule:
  #     interval: "weekly"
  #
  # - package-ecosystem: "npm"
  #   directory: "/"
  #   schedule:
  #     interval: "weekly"
